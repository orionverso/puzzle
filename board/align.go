package board

// we define name to align what should be considered the same piece of the system,
// therefore interchangeable

var writeandsave string = "LambdaStorage"
var saver string = "Storage"
var writer string = "Lambda"

var triggerwriteandsave string = "TriggerLambdaStorage"
var trigger string = "Trigger"
